# Oasis Local Development

Default mode is offline/local:

```text
GAME_GAME_GO_MODE=offline
```

Supported values:

```text
offline
local
oasis_testnet
```

Local mode uses anonymous local identity, in-memory match registry and
deterministic local result verifier. It requires no network, wallet or private
key.

