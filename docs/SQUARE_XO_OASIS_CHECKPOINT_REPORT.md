# SquareXO + Oasis Checkpoint Report

Current status note: later blockchain prephase work is tracked in
`docs/BLOCKCHAIN_PREPHASE_AUDIT.md`, `docs/RESULT_COMMITMENT_SPEC.md` and
`docs/THREAT_MODEL.md`. Local adapters, SquareXO replay verification, result
commitments and the Python ROFL service boundary are now unit-tested; live
Oasis/ROFL deployment remains unvalidated.

## CHECKPOINT 0 STATUS

1. Objective: audit Game Game Go and SquareXO source before integration.
2. Repositories and revisions inspected:
   - Game Game Go: `2e9ec05baef8934a7d50cfb8806eead562f802fe`.
   - SquareXO: `fffd7c2030443e60e90572277f2968f70acbf21d`.
3. License findings:
   - Game Game Go: MIT.
   - SquareXO: Apache-2.0.
4. Source findings:
   - SquareXO is TypeScript monorepo with `game-core`, frontend, backend and Hardhat contracts.
   - Core rules are in `packages/game-core/src/engine`.
   - Contract `SquarexoMatch.sol` includes native-value bet/pot/reward logic.
   - Backend has Sapphire submit-result service using private-key signer config.
   - Windows clone reports case collision: `GameCanvas.tsx` vs `gameCanvas.tsx`.
5. Files created: `.external/SQUAREXO` clone for audit.
6. Files modified: none in source during audit.
7. Architectural decisions:
   - Port deterministic game core to Python instead of embedding TS/web runtime.
   - Do not integrate source wagering contract into user flow.
8. Security and wagering safeguards:
   - Wagering code identified and documented.
   - Integration proceeds with `NO_STAKE` and local/mock modes.
9. Compatibility preserved: Color Wars untouched during audit.
10. Tests executed:
    - `pnpm -r test` in SquareXO clone.
11. Test results:
    - blocked by `spawn EPERM` before successful completion.
12. Runtime validation: not run.
13. Oasis validation level: `DESIGN_ONLY`.
14. Known limitations: Source tests could not be executed in this sandbox.
15. Next checkpoint: integration specification and conformance vectors.

## CHECKPOINT 1 STATUS

1. Objective: define SquareXO domain spec, canonical state/move/result and conformance vectors.
2. Repositories and revisions inspected: SquareXO `game-core` files and tests.
3. License findings: Apache-2.0 allows port with attribution.
4. Source findings:
   - edge keys are direction-independent.
   - completing a square keeps the turn.
   - terminal condition is inferred from all edges being taken.
5. Files created:
   - `tests/conformance/square_xo_vectors/basic_vectors.json`
   - `docs/SQUARE_XO_CONFORMANCE.md`
6. Files modified: docs.
7. Architectural decisions:
   - canonical serialization uses sorted JSON and SHA-256, never Python `hash()`.
8. Security and wagering safeguards: conformance excludes betting/settlement.
9. Compatibility preserved: no platform dependency on SquareXO internals except registry setup.
10. Tests executed: included in final validation.
11. Test results: conformance test passed in final subset.
12. Runtime validation: static/domain only.
13. Oasis validation level: `STATIC_VALIDATION`.
14. Known limitations: vectors are manually derived from audited source tests, not generated by source runner.
15. Next checkpoint: SquareXO domain core.

## CHECKPOINT 2 STATUS

1. Objective: port SquareXO deterministic domain core.
2. Repositories and revisions inspected: SquareXO game-core at audited revision.
3. License findings: attribution added in `THIRD_PARTY_LICENSES.md`.
4. Source findings: immutable transition style can be represented with frozen dataclasses.
5. Files created:
   - `src/games/square_xo/domain/*`
   - `src/games/square_xo/application/*`
   - `tests/games/square_xo/test_domain_rules.py`
   - `tests/games/square_xo/test_replay.py`
   - `tests/games/square_xo/test_conformance_vectors.py`
6. Files modified: none outside integration paths.
7. Architectural decisions:
   - domain has no Pygame, RPC, wallet, sockets, env reads or system clock dependency.
8. Security and wagering safeguards:
   - domain has no stake/token/value concept.
9. Compatibility preserved: Color Wars logic tests continue to pass.
10. Tests executed: final subset.
11. Test results: included in `55 passed`.
12. Runtime validation: domain only.
13. Oasis validation level: `UNIT_TESTED`.
14. Known limitations: no AI/bot mode for SquareXO.
15. Next checkpoint: local mini-game.

## CHECKPOINT 3 STATUS

1. Objective: make SquareXO a local playable mini-game.
2. Repositories and revisions inspected: SquareXO frontend canvas behavior.
3. License findings: frontend not copied verbatim.
4. Source findings: source frontend defaults to 4x4 and mouse-nearest-edge selection.
5. Files created:
   - `src/games/square_xo/game.py`
   - `src/games/square_xo/manifest.py`
   - `src/games/square_xo/runtime/*`
   - `src/games/square_xo/view/*`
6. Files modified:
   - `src/platform/app.py`
   - `tests/platform/test_app_lifecycle.py`
7. Architectural decisions:
   - SquareXO launch scene owns board size and local/mock options.
8. Security and wagering safeguards:
   - launch scene labels no real-money wagering.
   - default `stake_mode` is `NO_STAKE`.
9. Compatibility preserved:
   - Color Wars and demo remain registered.
10. Tests executed: final subset.
11. Test results: registry/session contract tests pass.
12. Runtime validation: live Pygame run not possible because environment lacks `pygame`.
13. Oasis validation level: `UNIT_TESTED`.
14. Known limitations: local session could not be manually played in this environment.
15. Next checkpoint: blockchain ports/local adapters.

## CHECKPOINT 4 STATUS

1. Objective: add shared blockchain/Oasis ports and local adapters.
2. Repositories and revisions inspected: SquareXO backend blockchain service and contract.
3. License findings: no code copied from contract/backend into platform adapters.
4. Source findings:
   - source backend uses Sapphire ethers signer and private-key backend signer.
   - Game Game Go must not require private keys for local play.
5. Files created:
   - `src/platform/blockchain/**/*`
   - `tests/platform/blockchain/*`
6. Files modified:
   - `src/platform/context.py`
   - `src/platform/scenes/settings_scene.py`
7. Architectural decisions:
   - local adapters are real development implementations, not test-only mocks.
8. Security and wagering safeguards:
   - no private key storage.
   - config defaults to offline.
9. Compatibility preserved:
   - platform can boot without Oasis config.
10. Tests executed: final subset.
11. Test results: local adapter/config tests pass.
12. Runtime validation: static/domain only.
13. Oasis validation level: `UNIT_TESTED`.
14. Known limitations: no live RPC or wallet signer implementation.
15. Next checkpoint: Sapphire contracts.

## CHECKPOINT 5 STATUS

1. Objective: add platform no-wager contract MVP.
2. Repositories and revisions inspected: source `SquarexoMatch.sol`.
3. License findings: new contracts are original platform contracts, not copied from wagering contract.
4. Source findings:
   - original SquareXO contract has bet/payout mechanics and is not reused.
5. Files created:
   - `contracts/foundry.toml`
   - `contracts/src/GameGameGoMatchRegistry.sol`
   - `contracts/src/GameGameGoResultRegistry.sol`
   - `contracts/src/GameGameGoAchievements.sol`
6. Files modified: none.
7. Architectural decisions:
   - contracts store commitments/hashes only.
8. Security and wagering safeguards:
   - no payable functions.
   - no tokenomics, withdrawal, payout or reward transfer.
9. Compatibility preserved: not wired into local gameplay by default.
10. Tests executed: no `forge test`; toolchain not validated here.
11. Test results: not run.
12. Runtime validation: not applicable.
13. Oasis validation level: `STATIC_VALIDATION`.
14. Known limitations: Foundry tests are placeholders.
15. Next checkpoint: ROFL verifier.

## CHECKPOINT 6 STATUS

1. Objective: add ROFL verifier scaffold.
2. Repositories and revisions inspected: SquareXO deterministic rules.
3. License findings: no external ROFL code copied.
4. Source findings: replay verification is deterministic and suitable for ROFL.
5. Files created:
   - `rofl/game-service/*`
6. Files modified: docs.
7. Architectural decisions:
   - keep ROFL scope to replay verification/result attestation.
8. Security and wagering safeguards:
   - ROFL scaffold has no wallet seed management and no payout logic.
9. Compatibility preserved: local verifier remains primary implementation.
10. Tests executed: compileall includes ROFL Python placeholder.
11. Test results: compileall passed.
12. Runtime validation: no container build.
13. Oasis validation level: `STATIC_VALIDATION`.
14. Known limitations: not container-built, not bundle-built, not deployed.
15. Next checkpoint: online/testnet integration deferred.

## CHECKPOINT 7-9 STATUS

1. Objective: final local validation, hardening docs and limitations.
2. Repositories and revisions inspected: both audited revisions above.
3. License findings: `docs/THIRD_PARTY_LICENSES.md` added.
4. Source findings: Game Game Go now lists SquareXO through registry and local adapters.
5. Files created:
   - Oasis docs, security docs, SquareXO docs, `.env.example`.
6. Files modified:
   - `README.md`
   - `docs/ARCHITECTURE.md`
   - `docs/TESTING.md`
   - `docs/ROADMAP.md`
7. Architectural decisions:
   - online/testnet flows remain disabled until non-blocking jobs, wallet UX and live toolchains are validated.
8. Security and wagering safeguards:
   - secret scan by text search found only documentation/prohibition references.
9. Compatibility preserved:
   - non-Pygame Color Wars tests still pass.
10. Tests executed:
   - `py -m compileall -q src tests rofl\game-service\src rofl\game-service\tests`
   - `py -m pytest tests\platform tests\games tests\game_logic tests\ai -q -p no:cacheprovider`
   - `py -m pytest -q -p no:cacheprovider`
11. Test results:
   - compileall passed.
   - subset: `55 passed`.
   - full pytest blocked by 11 collection errors because current Python lacks `pygame`.
12. Runtime validation:
   - `python -m src.main` not run; Python/Pygame runtime unavailable in this environment.
13. Oasis validation level: `UNIT_TESTED` for local adapters; `STATIC_VALIDATION` for Sapphire/ROFL live integrations.
14. Known limitations:
   - no Foundry test execution.
   - no ROFL container build.
   - no Oasis testnet deployment.
   - no live Pygame manual play.
15. Next checkpoint:
   - run full suite and manual play in a Pygame-capable environment, then implement non-blocking testnet integrations.
