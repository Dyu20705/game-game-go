# Contributing

- Keep game-specific rules, state and presentation inside the owning game
  package.
- Register games through `src.platform.bootstrap`.
- Do not reintroduce legacy root imports such as `src.ai` or `src.view`.
- Run lint, tests, smoke and link checks before opening a pull request.
- Do not add claims of live blockchain deployment unless a deployment artifact
  and verification steps are committed.
